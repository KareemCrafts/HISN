[build]
builder = "nixpacks"

[deploy]
startCommand = "python -m src.dashboard.app"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"

[env]
FLASK_HOST = "0.0.0.0"
FLASK_PORT = "8080"
